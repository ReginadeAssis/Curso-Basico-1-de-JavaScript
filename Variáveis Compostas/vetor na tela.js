let valores = [8,1,7,4,2,9]
console.log(valores[0])

//for(let pos=0; pos < valores.length; pos ++) {
   // console.log(`A posição ${pos} tem o valor ${valores[pos]}`)}

    //personaliza a saída das variáveis

//for(let pos in valores ){console.log(`A posição ${pos} tem o valor ${valores[pos]}`)}
//para cada posição dentro de num, para cada posição dentro da 
//varíavel composta eu mostro o num pos
//essa forma só funciona para array e objetos

